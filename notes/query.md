📦 Как формируется и используется query в проекте

🧠 Кратко:

query — это объект, который собирается из состояния фильтров и пагинации, а потом используется для запроса данных с сервера. Он проходит через несколько функций:

⸻

🔁 Полный путь query:

1. collectState() → собирает данные формы и фильтров

const state = collectState(container);

	•	Возвращает объект с полями:
	•	seller_id
	•	customer_id
	•	date_gte, date_lte
	•	page, rowsPerPage

2. applyPagination(query, state, action) → добавляет page и limit

const query = applyPagination(query, state, action);

	•	Добавляет или обновляет поля:
	•	page — номер текущей страницы
	•	limit — сколько записей на странице

3. api.getRecords(query) → отправляет запрос на сервер

const { items, total } = await api.getRecords(query);

	•	Преобразует pagedQuery в строку через URLSearchParams
	•	Делает запрос по адресу:

https://.../records?page=2&limit=10&seller_id=123&customer_id=456

	•	Возвращает результат:

{
  total: 50,
  items: [ ... ]
}


⸻

🧭 Итого:
	•	query — это динамически собираемый объект.
	•	Его создают из формы (collectState()), дополняют для пагинации (applyPagination()), и используют в запросе (getRecords()).
	•	Все изменения происходят до запроса — сервер получает уже полный набор параметров.

⸻

✅ Пример итогового query:

{
  seller_id: "123",
  customer_id: "456",
  page: 2,
  limit: 10
}


⸻
