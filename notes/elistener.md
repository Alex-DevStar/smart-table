📌 submit + e.submitter — Шпаргалка

🧠 Что происходит:
Когда в форме нажимают submit, можно узнать по какой кнопке это произошло — через e.submitter.

⸻

✅ Пример HTML:
<form id="my-form">
  <input type="text" name="name" placeholder="Имя" />
  <button type="submit" data-action="save">💾 Сохранить</button>
  <button type="submit" data-action="delete">🗑 Удалить</button>
</form>

✅ Обработка в JS:

form.addEventListener('submit', (e) => {
  e.preventDefault();                   // отменяем стандартную отправку формы

  const btn = e.submitter;              // кнопка, по которой был сабмит
  const action = btn.dataset.action;    // читаем её data-action

  if (action === 'save') {
    // логика сохранения
  } else if (action === 'delete') {
    // логика удаления
  }
});

🔍 Зачем использовать e.submitter:
	•	Форма может быть одна, а кнопок много.
	•	Поведение зависит от кнопки.
	•	Логика становится чистой, без лишних if’ов по id или class.

⸻

💡 Пояснение:
	•	e — объект события (submit, click, change и т.д.)
	•	e.submitter — именно та кнопка, которая вызвала отправку формы
	•	e.currentTarget — элемент, на котором висит обработчик (в нашем случае — форма)