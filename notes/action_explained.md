
# 📘 Пояснение: Что такое `action` и как она работает в проекте

---

## 🧩 Где возникает `action`

В коде `initTable` внутри `submit`-обработчика формы:

```js
root.container.addEventListener('submit', (e) => {
    e.preventDefault();
    onAction(e.submitter); // <-- Вот здесь
});
```

Здесь `e.submitter` — это DOM-элемент кнопки (например, `<button name="next">`), по которой был произведён `submit`. Это и есть `action`.

---

## 🎯 Что передаётся в `render(action)`

Когда вызывается `onAction(action)` (где `onAction = render`), то `action` попадает как аргумент в `render`:

```js
function render(action) {
    ...
    result = applyPagination(result, state, action);
    ...
}
```

---

## ⚙️ Как `action` используется в пагинации

Функция `initPagination` возвращает функцию, в которую `render` передаёт `action`. Внутри неё:

```js
if (action) switch(action.name) {
    case 'prev': page = Math.max(1, page - 1); break;
    case 'next': page = Math.min(pageCount, page + 1); break;
    case 'first': page = 1; break;
    case 'last': page = pageCount; break;
}
```

То есть по `action.name` определяется тип действия, и меняется текущая страница.

---

## 🧠 Ключевое понимание

- `action` — это **DOM-элемент** кнопки, с которой произошло действие (submit).
- `action.name` — это значение `name=""` у этой кнопки, по которому **обнаруживают**, что хочет сделать пользователь: перейти на следующую, предыдущую страницу и т.д.
- Если `action` не передан (например, при изменении поля, а не клике по кнопке), то `render()` отрабатывает без изменения текущей страницы.

---

## 📌 Пример кнопки

```html
<button type="submit" name="next" class="pagination-button">
```

Здесь:
- `type="submit"` — делает так, чтобы клик по кнопке сработал как отправка формы.
- `name="next"` — это `action.name`.

---

## ✅ Вывод

- `action` — это **переданный DOM-элемент**, чаще всего кнопка.
- Его `name` используется как **команда** для определения логики пагинации.
- Это позволяет **в единой функции `render`** отрабатывать как изменения формы, так и клики по кнопкам.

