🔹 Собирает все значения из формы в таблице (sampleTable.container)
🔹 Преобразует строки в числа (rowsPerPage, page)
🔹 Возвращает один объект состояния, готовый для логики пагинации, рендера и запроса

на выходе получаем обьект такого вида
      {
  ...state,         // сохраняем все остальные поля из формы
  rowsPerPage,      //вытаскиваем селект с таким именем для использования  добавляем в числовом виде
  page              // добавляем номер текущей страницы в числовом виде
};

{
  поле: значение
  ...
  ...
  rowsPerPage: 25,
  page: 2
}