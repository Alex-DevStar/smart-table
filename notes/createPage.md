🔹 createPage(el, page, isCurrent) — функция создания кнопки страницы

📌 Назначение:
Создаёт DOM-элемент для конкретной кнопки пагинации, используя шаблон и данные о номере страницы.

🧱 Аргументы:
	•	el — DOM-элемент, клонированный из pageTemplate, содержащий <label> с input и span;
	•	page — номер страницы, который будет вставлен как значение и текст;
	•	isCurrent — булево значение (true/false), показывает, активна ли эта страница.

⚙️ Логика работы:
const input = el.querySelector('input');
const label = el.querySelector('span');

input.value = page;
input.checked = isCurrent;
label.textContent = page;
return el;

📥 На вход: клонированный шаблон, нужный номер и статус
📤 На выход: обновлённый DOM-элемент кнопки

pages.replaceChildren(...visiblePages.map(pageNumber => {
    const el = pageTemplate.cloneNode(true);
    return createPage(el, pageNumber, pageNumber === page);
}));
💡 Важно:
Функция не занимается вставкой в DOM напрямую — только возвращает готовый элемент. Вставка происходит в .replaceChildren(...).

⸻
