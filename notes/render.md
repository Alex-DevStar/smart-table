📘 Объяснение работы render(action) в контексте таблицы

⸻

🔧 Цель функции render(action)
Функция render используется для перерисовки таблицы в ответ на действия пользователя:
	•	переключение страницы
	•	изменение количества строк
	•	фильтрация / поиск

Она не сама отрисовывает DOM, а вызывает метод отрисовки таблицы, передав в него подготовленные данные.

⸻

🧱 Структура функции render

function render(action) {
    let state = collectState(); // Собираем состояние из input'ов таблицы
    let result = [...data];     // Копируем данные для дальнейшей фильтрации / сужения

    result = applyPagination(result, state, action); // Обрезаем данные под текущую страницу

    sampleTable.render(result); // Отрисовываем таблицу по суженному массиву
}


⸻

🔗 Как всё связано
	•	render — это твоя пользовательская функция: собирает состояние и решает, что делать с данными
	•	sampleTable.render — это метод, который отрисовывает строки в DOM (он знает про шаблоны и куда что вставлять)

Функции просто называются одинаково, но это разные вещи.

⸻

⏱ Когда вызывается render
	•	Когда ты кликаешь на кнопку пагинации → вешается обработчик → вызывается render(action)
	•	Когда ты меняешь количество строк на странице — тоже вызывается render

⸻

🧠 Как передаётся render в таблицу

const sampleTable = initTable(
  {
    tableTemplate: 'table',
    rowTemplate: 'row',
    before: [],
    after: ['pagination']
  }, 
  render
);

render передаётся как второй аргумент в initTable, чтобы таблица знала, что делать при изменениях.

⸻

📦 Что делает applyPagination(...)

const skip = (page - 1) * rowsPerPage;
return data.slice(skip, skip + rowsPerPage);

Она берёт нужный фрагмент данных (например, строки 11–20), чтобы показать только их.

⸻

🧩 Пример полного потока
	1.	Пользователь нажимает “следующая страница”
	2.	Срабатывает addEventListener → вызывает render('next')
	3.	render собирает state с текущей страницы, лимита и т.п.
	4.	applyPagination возвращает нужный фрагмент data
	5.	sampleTable.render(result) отрисовывает эти строки в DOM

⸻

✅ Вывод
	•	render — логика, что делать с состоянием
	•	sampleTable.render() — механика DOM-отрисовки
	•	Всё связно через initTable(..., render)

Ты управляешь данными — таблица просто их красиво рисует 💡