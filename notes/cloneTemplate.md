## 🧠 cloneTemplate — шпаргалка

**Назначение:**  
Клонирует `<template>` по `id` и возвращает объект с доступом к ключевым элементам внутри (по `data-name`). 
<template id="row">

---

### 📦 Что возвращает: обьект такого вида

```js
{
  container: первый потомок DOM-элемента по его template id !
  <div class="table-row">
    <div data-name="rows">...</div><div>...,  // копия содержимого шаблона
  elements: {              // объект: ключ = значение data-name
    rows: <div data-name="rows">...</div>,
    total: <div data-name="total">...</div>,
    ...
  }
}


⚙️ Важные детали
	•	Использует template.content.firstElementChild.cloneNode(true) — полное копирование с потомками.
	•	Все элементы с data-name превращаются в свойства объекта elements, где ключ — значение data-name, а значение — сам DOM-элемент.
	•	Удобно для динамической вставки данных по ключам.
