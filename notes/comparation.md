🧩 compare — что это такое?

compare(source, target, rulesList)

Это функция сравнения, которая проходит по ключам target и сверяет их с source,
используя набор правил (rulesList — массив функций).

💡 Что она делает пошагово:
	1.	Проверяет, что source и target — объекты.
	2.	Проверяет, что rulesList — массив и он не пустой.
	3.	Идёт по каждому ключу из target.
	4.	Для каждого ключа:
	•	Берёт значение из target (targetValue)
	•	Берёт значение из source (sourceValue)
	•	Применяет каждое правило из rulesList:
	•	Если правило говорит skip: true → пропускаем это поле.
	•	Если правило вернуло result: true/false → используем этот результат.
	•	Если continue: true → идём к следующему правилу.
	5.	Если хоть одно поле не прошло проверку — compare вернёт false.
	6.	Если всё прошло — true.

То есть это как фильтр: «Подходит ли этот source под шаблон target по заданным правилам?»

🧠 rulesList — это список правил, каждое из которых:
function rule(key, sourceValue, targetValue, sourceObj, targetObj) {
  return {
    result: true/false,
    skip: true/false,
    continue: true/false
  }
}

🔁 createComparison — что это?

createComparison(ruleNames, customRules = [])

Функция, которая возвращает новую функцию сравнения — с заданными правилами.

📦 Что внутри:
	1.	Получает массив имён правил (ruleNames) и доп. правила (customRules).
	2.	Превращает ruleNames в реальные функции правил:
	•	Если ruleName === "skipNonExistentSourceFields" → передаёт в неё source
	•	Иначе просто вызывает rules[ruleName]()
	3.	Объединяет все правила в rulesList
	4.	Возвращает функцию (source, target) => compare(source, target, rulesList)

⸻
✅ Как использовать:
const compareFn = createComparison(['strictEqual', 'skipEmpty'], [myCustomRule]);

// Потом в коде:
const isMatch = compareFn(sourceObject, targetObject);

либо 